[phases.setup]
nixPkgs = ["nodejs-22_x"]

[phases.install]
cmds = ["cd blogs && npm ci"]

[phases.build]
cmds = ["cd blogs && npm run build"]

[start]
cmd = "cd blogs && npx vite preview --host 0.0.0.0 --port ${PORT:-3000}"

